[package]
name = "function-stream-catalog-storage"
version = "0.6.0"
edition = "2024"

[lib]
name = "function_stream_catalog_storage"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0"
function-stream-catalog = { path = "../catalog" }
parking_lot = "0.12"
rocksdb = { version = "0.21", features = ["multi-threaded-cf", "lz4"] }
